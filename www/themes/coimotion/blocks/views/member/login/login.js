ctrl.startup = function()  {
	/*
	var req = {url: '/admin/user/info'};
	__.api( req, function(data) {
		if (data.errCode === 0) {
			if (data.value.lastOrgID)
				window.location = "/bkIndex/menu/"+data.value.lastOrgID;
			else
				window.location = "/bkIndex/menu";
		}
	});
	 */
};