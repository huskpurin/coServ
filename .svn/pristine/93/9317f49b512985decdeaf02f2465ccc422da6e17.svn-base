<p style="display:none"><%=JSON.stringify(bi)%></p>
<div class="modal-header">
  <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
  <h4 class="modal-title">場次資訊</h4>
</div>
<div class="modal-body">
  <div class="spIDnInfo">
    <p>ID：<%=value.siID%></p>
    <form id="regForm" role="form">
      <div class="form-group col-md-6">
        <label for="title" class="control-label">梯次：</label>
        <input type="text" name="title" class="form-control" value="<%=value.doc.title%>"></input>
      </div>
    </form>
    <p>日期：<%=value.doc.summary%></p>
    <p>開始時間：<%=new Date(value.fromTime)%></p>
    <p>結束時間：<%=new Date(value.toTime)%></p>
    <p>開始報名時間：<%=new Date(value.regFromTm)%></p>
    <p>結束報名時間：<%=new Date(value.regToTm)%></p>
    <!-- TODO: 也許上正式時，attr汗plac應該互換(bcaz GPS) -->
    <p>地區：<%=value.attr%></p>
    <p>地點：<%=value.place%></p>
    <!-- -->
    <!-- 額外資訊 -->
    <p>人數：<%=value.capacity%></p>
    <!-- -->
  </div>
</div>
<div class="modal-footer">
  <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
  <button type="button" class="btn btn-primary">Save changes</button>
</div>
