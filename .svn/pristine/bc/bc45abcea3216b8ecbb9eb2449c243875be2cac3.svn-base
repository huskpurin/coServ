exports.run = function(inData, callback)  {
	var  rtnData = {errCode: 0};
	rtnData.value = {
		list: [
		{
			title: 'TITLE 1',
			body: 'Gives you scalable vector icons that can instantly be customized — size, color, drop shadow, and anything that can be done with the power of module. This page is served by a static block. That means contents are neither coming from a local module nor from a API service call.',
			img: '../index/img/img.png'
		},
		{
			title: 'TITLE 2',
			body: 'Gives you scalable vector icons that can instantly be customized — size, color, drop shadow, and anything that can be done with the power of module. This page is served by a static block. That means contents are neither coming from a local module nor from a API service call.',
			img: '../index/img/img.png'
		},
		{
			title: 'TITLE 3',
			body: 'Gives you scalable vector icons that can instantly be customized — size, color, drop shadow, and anything that can be done with the power of module. This page is served by a static block. That means contents are neither coming from a local module nor from a API service call.',
			img: '../index/img/img.png'
		},
		{
			title: 'TITLE 4',
			body: 'Gives you scalable vector icons that can instantly be customized — size, color, drop shadow, and anything that can be done with the power of module. This page is served by a static block. That means contents are neither coming from a local module nor from a API service call.',
			img: '../index/img/img.png'
		}
		]
	};
	callback( rtnData );
};
